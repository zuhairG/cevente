<%= form_for(@user_org) do |f| %>
  <% if @user_org.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_org.errors.count, "error") %> prohibited this user_org from being saved:</h2>

      <ul>
      <% @user_org.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :organization_id %><br>
    <%= f.number_field :organization_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
